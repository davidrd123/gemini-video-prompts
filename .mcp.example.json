{
  "_comment": "Merge into your MCP client's server configuration and replace REPO_PATH. Credentials load from this checkout's untracked .env or the inherited environment. Do not add empty env entries: they can shadow .env keys. The generation server installs optional OpenAI support, but Gemini remains the default. See README for client-specific registration.",
  "mcpServers": {
    "gemini-prompts": {
      "command": "uv",
      "args": [
        "--directory",
        "REPO_PATH",
        "run",
        "--extra",
        "openai",
        "gemini-prompts-mcp"
      ]
    },
    "media-analysis": {
      "command": "uv",
      "args": [
        "--directory",
        "REPO_PATH",
        "run",
        "media-analysis-mcp"
      ]
    }
  }
}
